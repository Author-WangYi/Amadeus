# Amadeus 音乐生成器 · 使用说明

> 一个**离线**运行的 AI 作曲工具。输入一段英文描述，输出一首可以编辑的 MIDI 曲子，并自动渲染成可以听的音频。
> 双击 `启动.bat` 即可使用，**不需要安装 Python、不需要安装任何依赖、不需要联网**。

---

## 一、这是个什么软件

### 它能做什么

你用英文描述想要的音乐——曲风、乐器、调式、拍号、速度、和弦走向、情绪——它给你生成一首完整的曲子。

输入：

```
A soothing pop song that evokes feelings of love and relaxation,
featuring a gentle blend of piano, flute, violin, and acoustic guitar.
Set in the key of C major with a 4/4 time signature, the piece moves
at an Andante tempo, creating a meditative and emotional atmosphere.
```

输出：

- `outputs/generated.mid` —— 标准 MIDI 文件，可以直接拖进任何音乐软件继续编辑
- `outputs/music/generated.wav` —— 已经渲染好的音频，双击就能听

### 它和"文字生成音乐"的 AI 有什么不同

它**不是**文字直接生成音频波形的端到端模型，而是一个**符号音乐（MIDI）生成框架**。这个区别很重要：

| | 端到端音频模型 | Amadeus（本软件） |
|---|---|---|
| 中间产物 | 无，直接出波形 | 有，先出 MIDI 乐谱 |
| 能否编辑 | 基本不能改 | **可以**，改音符、换乐器、改速度都行 |
| 输出内容 | 一段成品音频 | 一份可继续创作的乐谱 |
| 可控性 | 低，只能反复重抽 | 高，结构、和声都被显式建模 |

它的内部是三段式：

1. **自回归 Transformer** —— 决定"弹哪些音"，像作曲一样一个音一个音往下写
2. **离散扩散模型** —— 决定"每个音怎么弹"，负责时值、力度、演奏法等细节
3. **表征优化** —— 从多个候选里挑出Overall质量最好的那个

模型规模 1.7 亿参数，权重来自 HuggingFace 上的 `longyu1315/Amadeus-S`。

---

## 二、它原本是怎么运行的（以及为什么不能直接双击）

原始仓库 `longyu1315/Amadeus` 是作者在**自己的 Linux 服务器**上开发运行的，不是给普通人双击用的。原样拿到 Windows 上，**一定跑不起来**。原因分两类：

### 第一类：原仓库写死了服务器环境

| 位置 | 原本的写法 | 结果 |
|---|---|---|
| 音色库路径 | 写死 `/data2/suhongju/.../soundfont.sf2` | 渲染音频时必崩 |
| 文件名提取 | `path.split('/')[-1]` ×4 处 | Windows 用反斜杠，取不到文件名，WAV 被写进奇怪的嵌套目录，界面里找不到 |
| 一处无用导入 | `from selectors import EpollSelector` | 这是 Linux 专属模块，Windows 上直接 `ImportError`，程序启动即死 |
| 文本编码器 | 8 处写死 `from_pretrained('google/flan-t5-base')` | 运行时联网去 HuggingFace 拉 947 MB，国内必然超时 |
| FluidSynth 参数 | 选项写在位置参数之后 | FluidSynth 2.x 会解析失败 |
| 依赖清单 | `demo/requirements.txt` 只有 9 行且不锁版本 | 会装出与代码不兼容的新版本，直接崩 |

### 第二类：外网访问问题（中国大陆环境）

- `huggingface.co` 直连返回 502，必须走镜像
- GitHub 直连被挡
- PyPI 官方源 1.4 MB/s，得换国内镜像

### 所以原本的安装流程长这样

普通人要走完这些步骤才能用上：装 conda → 建环境 → 装 CUDA 版 PyTorch → 装十几个依赖并处理版本冲突 → 配镜像 → 下 4.9 GB 模型 → 改 6 处源码 → 才能运行。全程至少两三个小时，而且中间任何一步失败都得自己排查。

---

## 三、为了变成便携软件，做了什么

### 1. 把 Python 解释器装进包里

Windows 的虚拟环境（venv）**天生不能搬**：`.venv` 里的 `python.exe` 只是一个 274 KB 的启动器，它靠配置文件里的 `home` 字段去找真正的 Python。一旦换电脑或换路径，这个指向就失效了。

解决办法：从系统 Python 3.12.10 精简出一份运行时，放进项目内 `runtime\python\`（**30 MB / 662 个文件**）。

这里有个**最大的隐藏坑**：PyTorch 的 DLL 依赖 VC++ 运行库（`msvcp140.dll` 等），而 Python 自己目录里**不带**这些文件——它们只存在于 `C:\Windows\System32`。别人电脑上如果没装过 VC++ 运行库，程序会直接崩。所以特意把运行库一并补进了包内。

### 2. 让虚拟环境能自动适应新路径

`启动.bat` 每次启动时，用 `%~dp0` 取得自己当前所在位置，把虚拟环境的配置改写成「**当前目录\runtime\python**」。

于是：改文件夹名、换盘符、换电脑——全部自动适配。

### 3. 模型与音色库全部本地化

- `models/Amadeus-S/`（3.98 GB）—— 主模型权重 + **自带的音色库**
- `models/flan-t5-base/`（947 MB）—— 文本编码器

代码里所有联网加载的地方都改成了优先查找本地路径。启动时会设置 `HF_HUB_OFFLINE=1`、`TRANSFORMERS_OFFLINE=1`，**强制离线**，根本不会去联网。

### 4. 做了中文界面和双击入口

- `启动.bat` —— 双击即用，自动打开浏览器，不需要命令行知识
- `修复环境.bat` —— 出问题时双击，逐项自检，能自动修的自动修

### 5. 修掉了 6 处仓库 bug 和 3 处性能问题

除了上面表格里的兼容性修复，还做了三件优化：

| 问题 | 原本情况 | 改后 |
|---|---|---|
| 无效的编译调用 | `torch.compile(model)` 的返回值被丢弃，编译从未生效，白白拖慢启动 | 移除，生成速度从 13 → **17.6 token/s** |
| 文本编码器重复加载 | **每点一次「生成」都重新从磁盘读 947 MB** | 加缓存，只读一次 |
| 公网暴露 | `share=True` 会建立公网隧道，国内常卡死，还把界面暴露到公网 | 改为只监听本机 `127.0.0.1` |

### 6. 清理了 4.85 GB 冗余

| 项目 | 大小 | 说明 |
|---|---|---|
| pip 缓存 | 4.09 GB | 装依赖时的下载缓存，纯垃圾 |
| `torch/lib/*.lib` | 825 MB | C++ 静态链接库，Python 推理完全用不到 |
| `torch/include/` | 27.7 MB | C++ 头文件，用不到 |
| 其他零散 | ~25 MB | 代码检查工具等 |

包体从 **14.47 GB 降到 9.65 GB**。

---

## 四、怎么用

### 三步

1. **完整解压**压缩包成一个普通文件夹（⚠️ 不要在压缩软件里直接双击，必须先解压）
2. 双击 **`启动.bat`**
3. 等 2~3 分钟，浏览器会自动打开操作界面

就这样。**不需要装任何东西，不需要联网。**

### 界面怎么用

在文本框里用**英文**描述你想要的音乐，点「生成」，等约 2 分钟，结果会出现在 `outputs/` 目录里。

描述写得越具体效果越好，建议覆盖这几个维度：

- **曲风**：pop / jazz / classical / rock / electronic …
- **乐器**：piano, violin, flute, acoustic guitar, drum …
- **调式**：key of C major / A minor / C# minor …
- **拍号与速度**：4/4 time signature，Andante / Allegro …
- **和弦走向**：G, C, F, G, C …
- **情绪**：meditative, emotional, energetic, relaxing …

一个完整例子：

```
A lively and melodic pop rock song featuring piano, overdriven guitar,
electric drum and electric bass, set in a fast 4/4 tempo and the key of
C# minor, with a frequently recurring chord progression of D, A, C#m,
and F# that evokes a mix of emotion and love.
```

### 出问题的时候

双击 **`修复环境.bat`**。它会逐项检查解释器、依赖、显卡、模型文件、音色库、VC++ 运行库，并给出明确的处理指引。

---

## 五、⚠️ 必须注意的地方

### 使用前

| # | 注意事项 | 说明 |
|---|---|---|
| 1 | **必须完整解压** | 绝对不要在压缩包里直接双击 `启动.bat`，包内文件的相对路径关系会失效，必定失败 |
| 2 | **解压到短路径** | 建议类似 `D:\音乐生成器\`。项目内目录层级较深，Windows 路径默认 260 字符上限容易被顶爆 |
| 3 | **需要 64 位 Windows** | Windows 10 / 11 均可 |
| 4 | **预留至少 15 GB 磁盘** | 解压后约 10 GB，加上生成过程中产生的临时文件与音频 |
| 5 | **杀毒软件可能误报** | 包内含 PyTorch 的一批 `.dll`，部分杀软会拦截或直接删除。如果启动时报"找不到运行环境"，请检查杀软的隔离区 |

### 启动时

| # | 注意事项 | 说明 |
|---|---|---|
| 6 | **首次启动要等 2~3 分钟** | 实测 198 秒。正在把 2.3 GB 权重载入显存，**不是卡死**，请别急着关 |
| 7 | **黑色窗口不能关** | 那个窗口就是程序本体。关掉它等于关闭软件 |
| 8 | **窗口出现后浏览器会自动打开** | 如果没反应，手动在浏览器输入 `http://127.0.0.1:7860` |
| 9 | **端口 7860 不能被占用** | 如果提示端口被占用，先关掉占用的程序，或重启后再试 |

### 运行中

| # | 注意事项 | 说明 |
|---|---|---|
| 10 | **全程不需要联网** | 程序已强制离线模式。断网运行完全正常，联网也不会去下载任何东西 |
| 11 | **提示词必须用英文** | 文本编码器是英文模型，中文描述效果很差 |
| 12 | **生成一首约 2 分钟** | 这是 512 个音符 token 的正常耗时，耐心等待 |
| 13 | **界面和窗口只能开一个** | 同时开两个会争抢显卡显存，容易一起崩 |
| 14 | **生成过程中不要点第二次「生成」** | 会排队或直接报错，等当前这首出来再点下一首 |

### 性能相关

| # | 注意事项 | 说明 |
|---|---|---|
| 15 | **有 NVIDIA 显卡会快很多** | 自动使用 GPU，一首约 2 分钟 |
| 16 | **没有 N 卡也能跑** | 会自动退回 CPU 模式，但一首要等 **10 分钟以上**，且占用大量内存 |
| 17 | **AMD 显卡** | 本包内置的是 CUDA 版 PyTorch，AMD 显卡无法加速，等同于 CPU 模式 |

### 搬移与维护

| # | 注意事项 | 说明 |
|---|---|---|
| 18 | **整个文件夹可以随意搬** | 改名、换盘、拷到别的电脑——都没问题。`启动.bat` 会自动适配新路径 |
| 19 | **但不要单独移动内部文件** | 尤其是 `runtime\`、`.venv\`、`便携工具\`，它们必须保持在一起 |
| 20 | **不要手动改 `.venv\pyvenv.cfg`** | 它由 `启动.bat` 自动维护，手改反而会破坏 |
| 21 | **发现异常先跑 `修复环境.bat`** | 90% 的问题它能直接指出原因 |
| 22 | **想删掉 `.workbuddy\` 目录也可以** | 那是开发过程中的记录，与程序运行无关，删掉不影响使用 |

### 清理

`outputs/` 目录会随着使用不断堆积生成的曲子（MIDI + WAV）。长期使用后建议定期清理，避免占用过多磁盘。

---

## 六、目录导读

```
Amadeus/
├── 启动.bat                    ← 双击这个开始使用
├── 修复环境.bat                ← 出问题时双击这个
├── 软件介绍.md / 软件介绍.html  ← 你正在看的这份说明
├── 上手教程.md                 ← 详细的技术教程（含原理讲解与排错手册）
│
├── runtime/python/             ← 包内自带的 Python 运行环境（已补 VC++ 运行库）
├── .venv/                      ← Python 依赖环境（PyTorch、Transformers 等）
├── 便携工具/环境自检.py         ← 修复环境.bat 背后的检查逻辑
│
├── models/                     ← 模型文件（约 4.9 GB）
│   ├── Amadeus-S/              ← 主模型权重 + 音色库 soundfont.sf2
│   └── flan-t5-base/           ← 文本编码器
│
├── demo/Amadeus_app_CN.py      ← 中文操作界面（启动.bat 会打开它）
├── generate.py                 ← 命令行生成入口
├── Amadeus/                    ← 核心代码：模型结构、编码解码、音频渲染
├── tools/fluidsynth/           ← 用于把 MIDI 渲染成 WAV 的合成器
│
└── outputs/                    ← 生成结果
    ├── generated.mid           ← MIDI 文件（可编辑）
    └── music/generated.wav     ← 音频文件（可直接播放）
```

---

## 七、常见问题速查

| 现象 | 原因与处理 |
|---|---|
| 双击 `启动.bat` 一闪而过 | 没有完整解压，或文件被杀软删除。重新解压／检查隔离区 |
| 提示"找不到内置的 Python 运行环境" | `runtime\` 目录缺失，通常是解压不完整或被杀软拦截 |
| 提示"找不到依赖环境目录 .venv" | 同上，重新完整解压 |
| 报错缺少 `msvcp140.dll` 等 | VC++ 运行库问题。包内已附，若仍报错请检查是否被杀软删除 |
| 卡在"正在把模型载入显存"很久 | 首次启动正常需要 2~3 分钟。超过 5 分钟再排查 |
| 浏览器没自动打开 | 手动访问 `http://127.0.0.1:7860` |
| 提示端口被占用 | 关掉占用 7860 端口的其他程序后重试 |
| 生成特别慢（10 分钟以上） | 没有检测到 NVIDIA 显卡，已退回 CPU 模式 |
| 显示显存不足 | 关掉其他占用显卡的程序，或把生成长度调小 |
| 音频没生成，只有 MIDI | 音色库或 FluidSynth 出问题，跑一次 `修复环境.bat` |
| 中文提示词效果差 | 改用英文描述 |

以上都没解决？双击 `修复环境.bat`，把输出的内容留下来，它包含完整的诊断信息。

---

## 八、关于

- 模型权重：`longyu1315/Amadeus-S`（HuggingFace）
- 文本编码器：`google/flan-t5-base`
- 本便携包在原仓库基础上做了 Windows 兼容性修复、离线化、运行时内置与性能优化，**未改动模型结构与生成算法**，生成质量与原版一致。

祝创作愉快。
